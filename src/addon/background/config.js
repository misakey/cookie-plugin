import { ENGINE_VERSION } from '@cliqz/adblocker-webextension';

const RESOURCE_URL = 'https://static.misakey.com/plugin/beta';

export const ENGINE_URL = `${RESOURCE_URL}/engine_v${ENGINE_VERSION}.bytes`;
export const DATABASE_URL = `${RESOURCE_URL}/rulesPurposes_v${ENGINE_VERSION}.json`;
